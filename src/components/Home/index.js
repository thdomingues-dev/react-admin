import React from 'react';
import { Card, CardContent, CardHeader } from '@material-ui/core';

const Home = () => {
  return (
    <Card>
      <CardHeader title="Hey, Welcome!" />
      <CardContent>React-admin is here.</CardContent>
    </Card>
  );
};

export default Home;